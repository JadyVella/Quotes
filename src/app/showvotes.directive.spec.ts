import { ShowvotesDirective } from './showvotes.directive';

describe('ShowvotesDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowvotesDirective();
    expect(directive).toBeTruthy();
  });
});
